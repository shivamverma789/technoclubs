<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chapter Admin Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #0B0F1C;
            --secondary-bg: #111827;
            --text-light: #F3F4F6;
            --text-muted: #9CA3AF;
            --highlight: #3B82F6;
            --accent: #FACC15;
            --gradient-bg: linear-gradient(145deg, #3B82F6, #8B5CF6);
            --button-bg: linear-gradient(145deg, #FACC15, #FFD700);
            --card-bg: linear-gradient(145deg, #1E293B, #111827);
            --shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }

        body {
            background-color: var(--primary-bg);
            color: var(--text-light);
        }

        .dashboard-container {
            max-width: 800px;
            margin: 100px auto;
            padding: 30px;
            background-color: var(--secondary-bg);
            border-radius: 8px;
            box-shadow: var(--shadow);
        }

        .btn-create-event,
        .btn-back {
            background: var(--button-bg);
            color: black;
            border-radius: 5px;
            padding: 10px 20px;
            font-size: 16px;
            text-align: center;
            border: none;
        }

        .btn-create-event:hover,
        .btn-back:hover {
            background: var(--highlight);
            color: white;
        }

        h2,
        p {
            color: var(--text-light);
        }

        /* Position the back button at the top-left */
        .btn-back {
            position: absolute;
            top: 20px;
            left: 20px;
            background: var(--button-bg);
        }
    </style>
</head>

<body>
    <!-- Back Button -->
    <a href="/" class="btn btn-back">Back to Home</a>

    <div class="dashboard-container">
        <h2 class="text-center mb-4">Welcome, Chapter Admin</h2>
        <p class="text-center">You have full access to manage events for your chapter.</p>

        <!-- Create Event Button -->
        <div class="text-center">
            <a href="/events/create" class="btn btn-create-event">Create New Event</a>
        </div>

        <!-- View Events Button -->
        <div class="text-center">
            <a href="/events" class="btn btn-create-event mt-4">View Events</a>
        </div>
        <div class="text-center">
            <a href="/membership/67de50563ffba3c26cec013d/requests" class="btn btn-create-event mt-4">Pending
                Applications</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>