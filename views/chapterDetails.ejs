<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= chapter.name %> - Chapter Details</title>
    <link rel="stylesheet" href="/stylesheets/chapterDetails.css">
</head>
<body>

    <div class="container">
        <h2><%= chapter.name %></h2>
        <p class="description"><%= chapter.description %></p>

        <div class="team-section">
            <h3>Chapter Team</h3>
            <ul>
                <li><strong>President:</strong> <%= chapter.president ? chapter.president.name : "Not Assigned" %></li>
                <li><strong>Vice President:</strong> <%= chapter.vicePresident ? chapter.vicePresident.name : "Not Assigned" %></li>
                <li><strong>Coordinator:</strong> <%= chapter.coordinator ? chapter.coordinator.name : "Not Assigned" %></li>
            </ul>
        </div>

        <div class="members-section">
            <h3>Team Members</h3>
            <% if (chapter.teamMembers.length > 0) { %>
                <ul>
                    <% chapter.teamMembers.forEach(member => { %>
                        <li><%= member.name %></li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No team members yet.</p>
            <% } %>
        </div>

        <a href="/chapters" class="back-btn">Back to Chapters</a>
    </div>

</body>
</html>
